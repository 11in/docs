---
title: Filters
eleventyNavigation:
    parent: Scribe
    key: Filters
    order: 2
---
- `md` Renders the filtered content into Markdown.
- `md_inline` The same as `md`, except it uses the "inline" processor, the most notable difference of which is that it doesn't wrap everything in a `<p>`.
